#include <iostream>
#include <math.h>

using namespace std;

int main()
{

    // Define tridiagonal matrix with three 1D-vectors

    int *

    A =


    // Finding the matrix coefficients

    new_a[j][k] = a[j][k] - (a[j][m]*a[m][k])/ (double) a[m][m];

    // Finding coeffientist for the tilde_b
    new_b[j] = b[j] - (a[j][m]*b[m])/ (double) a[m][m];


    a[j][k] = new_a[j][k];

    b[j] = new_b[j];

    cout << "Hello World!" << endl;
    return 0;
}
